{"number":2,"title":"Session display Phase 3 — Detach (detachable windows)","prio":"P1","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/2","refs":[6,7],"body":"## Requirement\n\n**Session display Phase 3 — Detach: detachable session windows.** Move a session into its own OS window (draggable to a 2nd monitor), session keeps running; reattach back into the main window.\n\n- **Legacy stays the default** (non-breaking); detach is an **action**, not a third exclusive mode. Controlled via setting `global.sessionDisplayMode`.\n- Builds on **Phase 1 tabs (#6)** + **Phase 2 settings window (#7)** (both done) — uses the multi-window pattern proven there.\n- Central change: route `terminal-data` per session to the owning window (sidebar/attention events stay in the main window).\n\nDetailed design and implementation plan → comment.\n"}
{"number":100,"title":"Terminal: working-status line renders stacked/duplicated (rare) — distinct from #85 garble","prio":"P2","labels":["bug"],"url":"https://github.com/deadeye636/switchboard/issues/100","refs":[85,6015,5801],"body":"## Requirement\n\nThe Claude working-status line occasionally renders **stacked/duplicated** — several\nsuccessive status frames sit on top of each other (e.g. three `Hashing…` lines with\n*different* content: `22 24`, `16s ↓401`, `11s ↓138 · thought for 5s`) instead of the\nsingle line overwriting in place. A resize heals it. **Occurs rarely.** Seen on build\n`main @ 7e473e7` (which already carries the DPR-refit fix), WebGL off (DOM renderer).\n\nThis is **distinct from #85** (that was the horizontal char-drift *garble* = xterm.js#6015,\naddressed by the DPR-refit). This is **vertical duplication** — old status rows not cleared\nwhen the TUI rewrites the status line — a different mechanism.\n\n**Candidate:** xterm.js#5801 (OPEN) — \"ED2 (`\\x1b[2J`) inside a DEC 2026 synchronized update\nblock resets viewportY, causing scroll yank during streaming redraws.\" Matches stacked status\nframes during sync redraws. To be confirmed.\n\n**Expected:** a single, in-place-updating status line; no stacking.\n\n## Diagnosis (in progress)\n\nTemporary probe added on a debug branch (never merged): **F5** = pure `terminal.refresh(0, rows-1)`\n(render repaint), **F8** = `safeFit` (geometry re-fit) on the focused session. Press each on the\nstacked ghost:\n- F5 clears it → dirty-row render-ghost → a refresh-heal fixes it.\n- F5 fails, F8/resize clears it → geometry/re-fit class.\n- Neither → buffer/sync-viewport (xterm.js#5801) → upstream / deeper.\n\nKeep open until the mechanism is identified.\n"}
{"number":128,"title":"WebGL context loss leaves a stale fit (no re-fit, no repaint)","prio":"P2","labels":["bug"],"url":"https://github.com/deadeye636/switchboard/issues/128","refs":[6015,81],"body":"`WebglAddon.onContextLoss` (`public/terminal-manager.js`) disposes the addon and marks the DOM renderer as suggested, but never re-fits the terminal.\n\nxterm silently continues on its DOM renderer, whose cell metrics differ from WebGL's (xterm.js#6015). Every other renderer switch compensates: `loadTerminalWebgl()` calls `safeFit()` after loading, and `showSession()` re-fits on the next frame when it restores a context suspended by the grid's off-screen handling. The loss path does neither.\n\nResult: a visible terminal that loses its GL context keeps a fit computed for WebGL cell metrics while rendering with DOM metrics — the bottom row can clip, the same failure mode #81 traced to renderer swaps under fractional display scaling. It self-heals only on the next tab switch, when `showSession()` reloads WebGL and re-fits.\n\nRaising the GL-context budget to 32 narrows the window but does not close it: driver-side losses still occur.\n\n### Acceptance\n- `onContextLoss` re-fits the terminal and forces a full repaint.\n- The re-fit is deferred by one frame — cell metrics are reported only after the renderer swap has painted, matching the existing restore path.\n- No re-fit is attempted on a terminal destroyed between the loss and the frame.\n\nSplit out of #81 (found while re-reviewing its WebGL invariants)."}
{"number":146,"title":"Onboarding: first-time-launch welcome screen","prio":"P2","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/146","refs":[],"body":"Show a welcome screen on the very first launch to orient a new user, then never again unless reset.\n\n## Requirement\n\n- On first launch (fresh install — no global settings yet), show a welcome overlay: brief intro to the app + primary actions.\n- **Actions**: \"Import settings\" (ties into settings import — migrate from another machine) and \"Open settings\" (first-time configuration), plus a Dismiss/close.\n- Dismissing persists a flag so it never shows again (store `welcomeDismissed` in the global settings blob via `merge-setting`, so it rides existing persistence + export/import).\n- Style matches existing dialogs (`new-session-overlay` pattern). UI text English.\n\nDepends on the settings export/import feature for the \"Import settings\" action.\n\nPlan/design in comments."}
{"number":172,"title":"Claude: resuming a session that a live process owns dies with a raw CLI error","prio":"P2","labels":["bug"],"url":"https://github.com/deadeye636/switchboard/issues/172","refs":[],"body":"## Problem\n\nSwitchboard offers **Resume** on every Claude session, including one that a **live process currently owns** — a background agent (`claude --bg`), or an interactive session running in a terminal outside Switchboard. Claude refuses to open such a session twice, so the spawn dies immediately:\n\n```\nSession <id> is currently running as a background agent (bg). Use `claude agents` to find and\nattach to it, or add --fork-session to branch off a copy.\n── session exited (code 1) ──\n```\n\nThe user clicked an action the app was never able to perform. The spawn path already refuses a launch it knows will fail — a disabled backend, a missing binary, a fork of an id the backend does not know yet (main.js, the guards before `buildLaunch`) — this case is simply not among them.\n\nTwo rows in the sidebar can also carry the **same name** (Claude writes the same auto `ai-title` into both transcripts when a background agent and an interactive session work on the same topic), and nothing distinguishes them. It reads as a duplicate entry; it is two sessions, one of which is live and unresumable.\n\n## Requirement\n\n- Do not spawn a resume that cannot succeed. Refuse it before the PTY exists, with a sentence that says why and what to do instead — the way the other pre-spawn guards do.\n- Offer the alternative Claude itself names: **fork a copy** (`--resume <id> --fork-session`), which Switchboard already supports.\n- Make a session that is currently running elsewhere recognisable in the sidebar, so two identically named rows can be told apart.\n- Ownership must be asked of the **backend**, not hardcoded: a backend that cannot answer the question keeps today's behaviour.\n\n## Notes\n\n- Claude answers it directly: `claude agents --json` prints the live sessions (documented in its `--help` as \"for scripting; does not require a TTY\") — one entry per session with `sessionId`, `kind` (`background` | `interactive`), `pid`, `status`, `name`, `cwd`.\n- It costs **~1.5 s** per call, so it cannot go on the scan path uncached.\n- `~/.claude/jobs/<short-id>/state.json` records the same session (`template: \"bg\"`), but its `state` reads `done` while the daemon still holds the session — the job state is NOT a liveness signal. The agent list is."}
{"number":206,"title":"Add Codex support to scheduled tasks (Claude + Codex)","prio":"P2","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/206","refs":[],"body":"Scheduled tasks work only for Claude today — the scheduler is Claude-hardcoded (`schedule-runner.js` / `schedule-ipc.js`: `claude … -p`, schedule defs in `<project>/.claude/commands/schedule-<slug>.md`, pre-seed a Claude JSONL to `--resume` into).\n\nOf the other backends, **only Codex makes sense** for unattended scheduling: `codex exec` is a genuine agentic headless runner (tools/edits, multi-step), auto-persists a rollout the scanner already indexes, gives clean auto-approve (`-a never`), and authenticates from an env key without a human. **agy, pi and hermes are OUT of scope** (per the assessment comment) — their unattended-auth or headless semantics make a cron run fragile or unverified.\n\n**Goal:** after this, scheduled tasks support **Claude and Codex — and only those.**\n\n**Requirement:** add Codex as a schedulable backend, driven by the backend descriptor (no hardcoded backend argv), generalizing the scheduler just enough for these two.\n\n**Acceptance:**\n- Codex scheduled tasks work end-to-end: create → cron run (`codex exec` headless, `-a never`) → the run shows as a Codex session (its rollout is auto-indexed by the scanner — no pre-seed/resume needed).\n- The scheduler is no longer Claude-only: a descriptor hook (e.g. `buildScheduleLaunch`) supplies the headless invocation + auto-approve flags per backend; no backend id hardcoded outside its own folder (CLAUDE.md rule).\n- Schedule definitions live in a **backend-neutral store** (backendId + prompt + cron + per-backend option overrides), not only Claude's `.claude/commands/`. Slug-grouping of repeated runs (Claude-only field today) gets a Switchboard-side map.\n- Claude scheduling keeps working unchanged.\n- agy, pi, hermes are **not** added — explicitly out of scope (documented in the assessment).\n"}
{"number":207,"title":"Variable quick-pick: keyboard-driven (type-to-filter, arrow nav, Enter inserts + trailing space)","prio":"P2","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/207","refs":[],"body":"The variable quick-pick popover already opens from the focused terminal via the existing `insertVariable` hotkey (**Ctrl/Cmd+Shift+V**, rebindable) and from the terminal-header key button. But it is **mouse-only**: inside the popover only Escape does anything — there is no filtering and no keyboard selection. You have to click a row's Insert button.\n\n**Requirement:** make the quick-pick keyboard-driven, command-palette style, reusing the existing hotkey (no new binding):\n- A **filter input**, focused on open; typing filters the list live (case-insensitive substring over the variable name).\n- **Arrow ↑ / ↓** moves a highlighted selection through the filtered list (wrapping optional).\n- **Enter inserts** the highlighted variable's resolved value **followed by a single trailing space, no newline** (distinct from Send, which adds a newline).\n- **Escape** closes (unchanged). Send / Copy stay available (mouse, or a modifier on Enter).\n\n**Acceptance:**\n- Ctrl/Cmd+Shift+V opens the popover with the filter focused.\n- Typing filters; ↑/↓ moves the highlight; Enter inserts `<value> ` (value + one trailing space, no newline) into the terminal; Escape closes.\n- Secret variables resolve through the existing insert path (no plaintext leak change).\n- No new hotkey is added — the existing `insertVariable` binding is reused.\n"}
{"number":211,"title":"Projects admin: the last Claude-only bits belong behind a backend hook","prio":"P2","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/211","refs":[171],"body":"Per-project trust is already backend business (#171): every backend that has a gate declares\n`projectTrust: { get, getMany, set }`, and the ones that have none (Pi, Hermes, agy) say so instead\nof inventing one. `projects.js` routes through the descriptor and names no backend id.\n\nThe rest of the Projects admin never made that move. `projects.js` still `require`s `claude-config`\ndirectly and treats Claude's `~/.claude.json` as if it were the app's own store:\n\n- **Per-project meta is hard-wired to Claude.** `getProjectClaudeMeta` supplies the MCP-servers,\n  allowed-tools, last-cost and token columns. There is no hook, so for a project that only ever ran\n  Codex the table shows Claude's numbers (or blanks) with nothing saying which backend they describe.\n- **`r.trusted` is a legacy duplicate.** It carries Claude's trust specifically, next to the generic\n  `r.trust[backendId]` map that superseded it.\n- **Existence is defined as \"listed in `~/.claude.json`\".** `unlistedProjects` reads Claude's config\n  to decide whether a project is known at all — a project with only Codex/Pi/Hermes sessions is not.\n- **Remap and delete bypass the descriptor for Claude.** `renameProjectEntry` / `removeProjectEntry`\n  are called straight, while every other backend goes through `rewriteProjectPath` / `deleteSessions`.\n- **`claude-config.js` sits at the repo root**, not in `backends/claude/` with the rest of its backend.\n\nRequirement: the Projects admin must not know that Claude exists. Per-project meta becomes a declared\ncapability, so a backend without one contributes no columns rather than borrowing Claude's; the\nClaude-only paths above go behind the same descriptor the other backends already use; and\n`claude-config.js` moves into `backends/claude/` as that backend's own store access.\n\nAcceptance:\n- `projects.js` contains no `require` of a backend-specific module and no backend id.\n- A project with no Claude sessions renders without Claude's meta attributed to it.\n- Rows show meta per backend, or nothing where a backend declares none.\n- `npm test` green, including `test/backend-parity.test.js`.\n"}
{"number":40,"title":"Remote repo over SSH (treat like local)","prio":"P3","labels":["feature","source:supacode"],"url":"https://github.com/deadeye636/switchboard/issues/40","refs":[],"body":"## Requirement\n\n**Remote repo over SSH (treat like local)** _(Source: supacode)_ — open a repo/project on a remote host via SSH and manage it in Switchboard like a local one: session scan, terminal (PTY over SSH), file/diff access. **Large** — needs its own detailed plan (transport, auth via Bitwarden SSH agent, remote `~/.claude/projects` scan, PTY bridging, watcher replacement). Clarify scope/architecture before implementation.\n"}
{"number":43,"title":"Detect subagent spawn via .meta.json (not only .jsonl)","prio":"P3","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/43","refs":[],"body":"## Requirement\n\n**Detect subagent spawn via `.meta.json` (not only `.jsonl`)** — when spawning a task subagent, Claude Code immediately writes `<parent>/subagents/agent-<id>.meta.json` (`agentType`, `description`, `toolUseId`, `spawnDepth`), but the `.jsonl` transcript only once the agent produces output. A spawned agent that did not (yet) run to producing output (queued, parent turn ends/is interrupted) has **only** `meta.json` → `detectSubagentTransitions`/`enumerateSessionFiles` (which only scan `subagents/*.jsonl`) never see it (observed 2026-07-02: session `876c2b28`, \"Feldtexte W6\" agents — meta identical to runnable W5 agents, but without jsonl; parent last at 22:26, spawns at 22:28). **No special background-agents format, no data loss — pure visibility.** Fix: also scan `subagents/*.meta.json` → placeholder subagent row (name from `description`, status \"spawned/empty/aborted\") as soon as the meta is there; upgrade to a full row when the `.jsonl` arrives. Files: `read-session-file.js` (`enumerateSessionFiles`), `session-transitions.js`, `session-cache.js`, subagent rendering in `public/sidebar.js`.\n"}
{"number":84,"title":"Make the terminal LRU cap configurable","prio":"P3","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/84","refs":[81],"body":"The renderer keeps at most 12 live xterm instances (`TERMINAL_LRU_CAP` in `public/terminal-manager.js`); the least-recently-shown closed session beyond the cap is destroyed (soft cap: running and active sessions are never evicted).\n\nSince WebGL is the default renderer, the cap also serves as the GL-context budget (Chromium allows ~16 per process; a lost context falls back to the DOM renderer).\n\nExpose the cap as a setting for users with many parallel sessions and enough RAM. Constraints: keep 12 as the default; values above ~14 should hint that terminals beyond the GL budget silently fall back to the DOM renderer.\n\nDeferred from #81 (was rejected there as \"defer until the cap is hit\")."}
{"number":104,"title":"Evaluate: multi-sidebar panes","prio":"P3","labels":["feature","port"],"url":"https://github.com/deadeye636/switchboard/issues/104","refs":[],"body":"Evaluate porting aaaron `feat/multi-sidebar-panes` — multiple sidebar panes. Source: aaaronmiller/switchboard @ feat/multi-sidebar-panes."}
{"number":105,"title":"Evaluate: local Whisper voice input","prio":"P3","labels":["feature","port"],"url":"https://github.com/deadeye636/switchboard/issues/105","refs":[],"body":"Evaluate porting ivandobsky `feat/local-whisper-voice` — local Whisper voice control/input. Source: ivandobskygithub/switchboard @ feat/local-whisper-voice."}
{"number":107,"title":"Evaluate: brianstanley cursor-ui-refresh","prio":"P3","labels":["feature","port","source:brianstanley"],"url":"https://github.com/deadeye636/switchboard/issues/107","refs":[],"body":"Evaluate brianstanley `cursor-ui-refresh` — UI refresh. Source: brianstanley/switchboard @ cursor-ui-refresh. We already ported brianstanley's Variables + preview panel; check what's new/portable."}
{"number":113,"title":"Optional second tray icon showing Claude usage (live percentage)","prio":"P3","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/113","refs":[],"body":"Add an optional **second system-tray icon** that shows current Claude usage, enabled via Settings (off by default).\n\n## Requirement\n- A live-rendered tray icon showing the current usage percentage, styled like a usage monitor: a **colored progress ring** or a **badge** with the number baked into the icon, color-coded by the existing warn/critical thresholds (green → amber → red).\n- **Tooltip** with the details we already show in the status bar (5h / 7d, extra usage, reset time).\n- Small **context menu**: show/hide the window, open Stats, refresh, quit.\n- **Cross-platform**: macOS, Windows, Linux.\n- **Reuse the usage data the app already has** — the same values the status bar renders. **No separate polling loop / no extra network fetch** for the tray; it updates whenever the existing status-bar usage updates.\n- Enabled + styled via a **Settings** toggle (icon style ring/badge). When off, the second tray is not created; toggling off destroys it.\n\nReference behaviour: a small pystray + Pillow usage-monitor tray that draws a ring/badge with the percentage and threshold colors."}
{"number":117,"title":"Terminal: evaluate VSCode-style shell integration (OSC 633) for plain terminals","prio":"P3","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/117","refs":[87,114],"body":"Idea from the VSCode draw-path comparison (#87/#114 context). VSCode's shell integration injects a shell profile hook that emits OSC 633 sequences (command start / command end / exit code / cwd), enabling command tracking, \"run recent command\", per-command scrollback navigation, and precise prompt detection.\n\n**Requirement:** evaluate whether a slim variant is worth it for our **plain terminals** (not Claude sessions — the CLI is a fullscreen TUI where command tracking is meaningless): e.g. exit-code badges, jump-to-previous-command in scrollback, or more reliable \"shell is idle\" detection. Likely low value for this app's core use case; document a decision either way. Large feature — only pursue with a concrete user-facing benefit.\n"}
{"number":143,"title":"i18n: renderer localisation support layer (language-neutral, EN master)","prio":"P3","labels":["feature","source:kreaddis"],"url":"https://github.com/deadeye636/switchboard/issues/143","refs":[],"body":"Add a language-neutral localisation **support layer** to the renderer, so any user-facing UI string can be translated. This issue is about the **infrastructure**, not the translations themselves — English stays the master/source of truth; concrete languages are added on top afterwards.\n\n## Requirement (the support layer)\n\n- A global `t('area.key', { placeholders })` available to every renderer script — no build step, no module system (matches our vanilla `<script>` setup).\n- **English is the master.** Missing keys fall back to English, then to the key itself — the UI is never blank.\n- Static HTML localised via `data-i18n` / `data-i18n-title` / `data-i18n-placeholder` / `data-i18n-aria` attributes, filled on load.\n- Active language is a global setting (Settings UI). Changing it persists and re-renders every view in the new language.\n- Extract the existing English UI strings (sidebar tabs/chrome, session filters, settings labels/dialogs/tooltips) into the dictionary, keyed. No behaviour change while only EN exists.\n- Language-neutral by design: adding a language = adding one dictionary, no code changes.\n\n## Rollout (separate follow-up issues, not this one)\n\nLanguage order once the layer lands: **EN (master) → DE → FR**. Each added language is its own translation task on top of this support layer.\n\n## Work organisation\n\nLarge, cross-cutting change (touches nearly every renderer file that emits text) → **own feature branch**.\n\nReference implementation for the layer: kreaddis `public/i18n.js` (~488 lines) — adapt the mechanism, ignore its specific fr strings. Upstream commit `6e4d127` (fr apostrophe fix) is only relevant once translations exist.\n\nPlan/design + string-extraction strategy go in comments."}
{"number":193,"title":"Session provenance: show where a session came from (clear heuristic + explicit parent links)","prio":"P3","labels":["feature"],"url":"https://github.com/deadeye636/switchboard/issues/193","refs":[],"body":"Switchboard shows sessions as a flat list, but many sessions are not fresh starts — they were spun out of an earlier session and lose that link in the UI. Surface a session's **provenance**: where it came from, so a session that continues another's work reads as such instead of as an unrelated new thread.\n\nThe relationship exists in the transcripts, but it is recorded differently per backend, and one case is not recorded at all:\n\n- **Claude `/clear`** starts a brand-new session with a new id and writes **no** back-link — the only trace is the `/clear` command at the head of the child file. Provenance here can only be **heuristic**.\n- **Claude / Codex compaction** stays in the *same* session and is already linked (`logicalParentUuid` + `isCompactSummary` for Claude; an inline `compacted` row with `replacement_history` for Codex) — no heuristic needed.\n- **Pi** writes an explicit `parentSession` (full path to the parent transcript) in its session header.\n- **Hermes** carries an explicit `parent_session_id` column.\n\nRequirement:\n\n1. Where a backend records lineage explicitly (Pi `parentSession`, Hermes `parent_session_id`), show it as a **hard** link (\"continued from …\").\n2. For **Claude `/clear`**, where no link exists, derive a **soft** heuristic guess and label it as a guess, never as fact. Detect the boundary from `/clear` at the head of the child session; pick the most likely parent from same `cwd` + same `gitBranch` + the nearest preceding session that ended just before the child began. On ambiguity (parallel sessions in the same cwd), show nothing rather than guess wrong.\n3. Optionally surface compaction as \"continued after compaction\" from the links that already exist.\n\nBackend-neutral: the lineage source is a per-backend detail (a descriptor hook), not a hardcoded `if (backendId === …)` in the core. A backend with no lineage concept simply contributes nothing.\n"}
{"number":201,"title":"agy backend: add a usage capability (status bar + Usage & Notifications), like Claude/Codex","prio":"P3","labels":["feature","blocked-on-upstream"],"url":"https://github.com/deadeye636/switchboard/issues/201","refs":[191],"body":"The `agy` backend ships without a `usage` capability (#191), so it has no usage figure in the status bar and no entry under **Settings → Usage & Notifications** — unlike Claude and Codex. The descriptor documents this as a deferred follow-up (`backends/agy/index.js:165-167`): agy's quota is per-model with no clean local file (unlike Codex's `token_count`), so a real usage bar needs a network fetch. It currently ships `ready` without one, like Hermes and Pi.\n\n**Requirement:** agy exposes a `usage` capability matching the #191 contract — a `backends/agy/usage.js` with a `fetch()`, declared as `usage: { live, fetch }` in the descriptor — so its quota shows in the status bar and under Usage & Notifications the same way Claude and Codex do. Everything else derives from the descriptor (the poll loop in `main.js` already enumerates every `enabled` backend whose descriptor has `usage.fetch`).\n\n**Acceptance:**\n- With agy enabled, the status bar shows its usage and Settings → Usage & Notifications lists agy, consistent with Claude/Codex.\n- `test/backend-parity.test.js` and the usage tests stay green.\n"}
{"number":210,"title":"perf: run the live-session identity match (matchLiveSession) in the index worker","prio":"P3","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/210","refs":[209,199,208],"body":"## Problem\n\n#209 measured the on-main cost of `matchLiveSession` (the birth-time identity match that pairs a freshly\nspawned Axis-B session with the transcript the CLI just named) and cut it with a filename-derived birth\npre-filter — 82% off a 5000-transcript store. What is left is the part a filter cannot remove: the\n**`discoverSessions()` readdir walk itself**, plus the stat of every transcript born inside the skew margin.\n\nMeasured after the #209 fix, per watcher flush while a session is still unpaired, on the main thread:\n\n| transcripts | matchLiveSession |\n|---|---|\n| 500 | ~17 ms |\n| 2000 | ~27 ms |\n| 5000 | ~44 ms |\n\nBounded (it stops the moment the session pairs, normally after 1–2 flushes) and an order below the stall\n#199 removed — but it is the last store walk the main thread still does, and it grows with the store.\n\n## Requirement\n\nRun the identity match off the main thread, through the persistent index worker: main posts the match\nrequest (backendId + cwd + sinceMs + the claimed set), the worker runs `matchLiveSession` (it is\nElectron-free and the worker already resolves backends by id), main claims the reply.\n\nKnown costs to design for, none of them blockers, all of them the reason this is filed rather than done:\n\n- **`claimLiveRecord` becomes async**, and `updateBackendLiveStates` is today a synchronous loop that\n  re-keys `activeSessions` mid-iteration. The re-key would move to a later tick — the snapshot-to-avoid-\n  mutation logic has to be rethought.\n- **New race surface**, the same shape #208 needed guards for: a session that exits while its match is in\n  flight; two flushes posting two match requests for one session (needs per-session coalescing); a stale\n  reply arriving after another claimed the record.\n- **Latency**: the id binding and the first busy/idle push wait a worker round-trip, so a brand-new\n  non-Claude session shows its temp id a beat longer. Claude's temp→real transition already tolerates this.\n- **The walk is relocated, not removed** — the worker then pays it, and a match competing with the reconcile\n  and file lanes needs a sensible place in the priority queue.\n\n## Acceptance\n\n- No store walk or transcript read runs on the main thread in the backend-store watcher flush.\n- A freshly launched Codex/Pi/agy session still binds to its real session id and shows live busy/idle,\n  without a restart; a resumed session still claims its record via `liveRefFor`.\n- `npm test` green.\n\nFollow-up to #209 (design record + the measured numbers: `docs/specs/11-performance.md`)."}
{"number":212,"title":"Renderer still names Claude in three places the descriptor should cover","prio":"P3","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/212","refs":[211],"body":"The descriptor migration got the settings surface right: `configFields` lives in `backends/<id>/index.js`,\nand `public/settings-panel.js` names no backend at all — it renders whatever the descriptor declares.\nThree places in the renderer never followed, and each still asks for a backend by name.\n\n- **`public/backends-panel.js` — `backend.id === 'claude'` gates the integrations block.** Opening a\n  backend's gear page appends `claudeIntegrationsHtml(attentionHooks)` when, and only when, the id reads\n  `claude`. The feature genuinely is Claude-only (the hooks live in Claude's own settings), but that is a\n  property of the backend, not something the panel should know. CLAUDE.md is explicit: `public/*.js`\n  contain no `if (backendId === 'codex')` and must not gain one — this is the same line with a different\n  id, and it is the one rule the settings surface otherwise keeps perfectly.\n- **`public/dialogs.js` — Claude gets a hardcoded popover icon.** The launch popover renders\n  `CLAUDE_POPOVER_ICON` for `claude` and routes every other backend through\n  `renderBackendIcon(backend.icon || backend.id)`. Claude has an icon like everyone else; the special\n  case is left over, not required.\n- **`|| 'claude'` as the fallback, roughly a dozen times** across `backends-panel.js` and `dialogs.js`.\n  Some of those are honest migration (an old profile record carries no `backendId`), but the rest are a\n  guess: with Claude disabled they resolve to a backend that cannot be launched. The repo already knows\n  better one function away — `backends-panel.js` falls back to the first LAUNCHABLE backend in one spot,\n  with a comment saying exactly why, and nothing else adopted it.\n\nRequirement: the renderer must not name a backend. Claude's integrations block becomes a declared\ncapability on the descriptor, in the shape `usage` and `projectTrust` already use, so Claude carries it\nand the others simply do not have it. The popover icon goes through the same path as every other\nbackend's. Each `|| 'claude'` is either a documented migration of an old record, or it resolves to the\nfirst launchable backend — no third kind survives.\n\nAcceptance:\n- `public/backends-panel.js`, `public/dialogs.js` and `public/settings-panel.js` contain no backend id\n  literal except where a comment states it is reading a legacy record.\n- With Claude disabled, the launch popover, the default-target select and the profile editor all resolve\n  to a launchable backend.\n- A backend that declares the integrations capability renders the block; one that does not renders\n  nothing there.\n- `npm test` green, including `test/backend-parity.test.js` and `test/backend-config-fields.test.js`.\n\nRelated: #211 (the same unfinished migration in `projects.js`).\n"}
{"number":213,"title":"main.js is ~4970 lines — split it into src/app/ the way session-cache.js was split","prio":"P3","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/213","refs":[199,214,215],"body":"`main.js` is ~4970 lines: 116 IPC handlers, 69 top-level functions, 38 requires. It is the main process's\nlifecycle, its PTY management, its file watching, its settings, its variables and secrets, its hook ingest\nand roughly a dozen unrelated IPC surfaces, in one file.\n\nIt is our own doing, not something inherited:\n\n```\nmain.js   ours 4967  |  haydng 1907  |  jbr 2138\n```\n\nWe more than doubled it. No other fork has this problem.\n\nThis has been done here before and it worked. `session-cache.js` was 1381 lines mixing four jobs; it is\nnow a **63-line façade** over `index-writes.js`, `backend-scan.js`, `backend-parse.js` and\n`projects-view.js` (#199 step 4). The façade kept the original name, so no caller had to change. That is\nthe pattern to repeat.\n\nThe seams are already drawn — `main.js` carries `// --- … ---` section headers, and they cluster with\nalmost no overlap:\n\n| Block | lines | approx |\n|---|---|---|\n| Terminal/PTY (`open-terminal`, input, resize, redraw, close, flow control) | 3394–4199 | 800 |\n| Watching (projects `fs.watch`, Axis-B stores, identity adoption, busy/idle) | 4199–4605 | 400 |\n| Variables + secret materialization | 2164–2487 | 320 |\n| Bookmarks / tasks / tags / handoffs | 2921–3254 | 330 |\n| Memories / plans / work files | 1370–2026 | ~500 |\n| Hook → attention ingest | 2487–2682 | 195 |\n| Stats / usage | 1445–1628 | 180 |\n| Notifications / tray | 627–727 | 100 |\n\n`open-terminal` alone is ~645 lines — a file's worth of code inside a single handler.\n\nRequirement: `main.js` becomes a thin composition root — app lifecycle plus the wiring that registers each\narea's handlers — with each block above in its own module. Split one block at a time, each landing green on\nits own. `main.js` keeps its name and its role as the entry point throughout, so nothing outside it moves\nin lockstep.\n\nDepends on #214 (the `src/` move). Do that first: splitting at the root and moving afterwards means\ntouching every new file twice.\n\n## Target — `src/app/`, filled by this issue\n\n```\nsrc/\n  main.js                    composition root — wiring, nothing else\n  app/\n    lifecycle.js             the real lifecycle (main.js ~L4645) + main-lifecycle.js' predicate\n    windows.js               BrowserWindow, zoom\n    notifications.js         tray, badge, native notifications\n    settings.js              settings IPC + the cascade + settings-transfer.js\n    variables.js             variables + secret materialization + env-refs.js\n    hooks.js                 hook → attention ingest\n    quit-guard.js\n    terminal/\n      spawn.js               open-terminal\n      io.js                  input, resize, redraw, close, flow control\n      shell-profiles.js  output-buffer.js  osc-busy.js\n      afk-timeout.js     live-record-notice.js\n  watch/\n    projects.js              fs.watch on the projects dir\n    stores.js                Axis-B store watcher\n    adopt.js                 identity adoption, busy/idle\n    trigger-watcher.js       (moved there by #214)\n```\n\nThe PTY-adjacent pure-logic modules (`osc-busy`, `output-buffer`, `afk-timeout`, `live-record-notice`,\n`shell-profiles`) join `app/terminal/` here rather than in #214 — they belong next to the code that\nconsumes them, which does not exist until this split creates it.\n\n## Acceptance\n\n- `main.js` registers and wires; it holds no IPC handler body of its own.\n- Each extracted module states its job in a header comment, and is requirable without Electron wherever it\n  has no Electron dependency (the scan/index leaves set the precedent).\n- No behaviour change: every IPC channel keeps its name and its shape.\n- `npm test` green after every individual step, not only at the end.\n- **The app starts and a session launches**, checked per step. A green suite is not evidence here — two\n  bugs shipped green because nobody clicked (CLAUDE.md).\n\nTiming: `main.js` collides with any branch holding it open. Land #214 first, then this, when nothing else\nhas it.\n\nRelated: #214 (the move this sits on), #199 (the same split one layer down, already done),\n#215 (the renderer's half).\n"}
{"number":214,"title":"Repo layout: move all app code under src/ and give it themed folders","prio":"P3","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/214","refs":[211,213,215],"body":"38 `.js` files sit flat in the repo root, next to `public/`, `workers/` and `backends/` — which are app\ncode too. Nothing in the listing tells you what is the main process, what is the renderer, what is\npersistence, and what is a one-function helper. The files themselves are fine: almost every one carries a\nheader stating its job, and the names are honest. They just have no home.\n\nTwo are in the wrong place outright:\n\n- **`backend-parse.js` and `backend-scan.js`** are generic Axis-B modules — the pure parse loop and the\n  generic store scanner — sitting at the root while `backends/` already holds exactly this kind of shared,\n  backend-neutral code (`file-store.js`, `index.js`, `presets.js`, `delete-sessions.js`, `rewrite-cwd.js`).\n- **`claude-config.js`** is Claude's own store access, at the root rather than in `backends/claude/`\n  (also #211).\n\nAnd one is named for a split that never happened: **`main-lifecycle.js` is five lines** holding a single\npredicate, `shouldUseSingleInstanceLock`, extracted only so it could be tested. The real lifecycle is in\n`main.js`. CLAUDE.md believes the name and tells every agent the main process is \"`main.js` +\n`main-lifecycle.js`: app lifecycle, IPC handlers, terminal (PTY) management…\" — so the map points at a\nfile that does none of it.\n\nRequirement: all app code moves under `src/`, in folders named after what they hold; the repo root keeps\nonly project metadata and tooling. This is a MOVE — no behaviour changes, no file is rewritten, no module\nis split. `main.js` and `public/`'s 69 files move as they are; taking them apart is #213 and #215, which\nland on top of this.\n\n## Target\n\n```\nswitchboard/\n├── package.json  README.md  CLAUDE.md  .gitignore\n├── .github/  build/  docs/  patches/  scripts/  test/\n└── src/\n    ├── main.js                    still the entry — split by #213\n    ├── preload.js                 the IPC boundary\n    │\n    ├── app/                       created by #213; empty until then\n    │\n    ├── watch/                     trigger-watcher.js\n    │                              (#213 adds the watchers it pulls out of main.js)\n    ├── db/                        db.js, stats-queries, search-query-util, sqlite-busy-retry\n    ├── index/                     session-cache (façade), index-writes, index-worker-client,\n    │                              search-worker-client, projects-view, folder-index-state\n    ├── session/                   session-transitions, session-backends,\n    │                              derive-project-path, encode-project-path\n    ├── projects/                  projects.js, project-registry\n    ├── servers/                   mcp-bridge, schedule-runner, schedule-ipc\n    │\n    ├── backends/                  as it is, plus:\n    │   ├── parse.js               ← backend-parse.js\n    │   ├── scan.js                ← backend-scan.js\n    │   ├── profiles.js            ← profiles.js\n    │   ├── usage-cache.js  metrics-bucket.js\n    │   └── claude/                + claude-config.js  (#211)\n    │\n    ├── workers/                   ← workers/\n    └── renderer/                  ← public/, moved FLAT (69 js + 3 assets). #215 structures it.\n```\n\nRoot after: `package.json`, `README.md`, `CLAUDE.md`, `.github/`, `build/`, `docs/`, `patches/`,\n`scripts/`, `test/`, `src/`.\n\nWhy process-first and not VS Code's domain-first (`src/vs/*/electron-main|electron-browser|common`):\nthe big Electron apps that do it that way are an order of magnitude larger, and their features straddle\nboth process sides. Ours grew already separated — `backends/` is pure main, `backends-panel.js` is pure\nrenderer — and at `contextIsolation: true` the boundary `preload.js` guards is the one that actually\nmatters. A domain axis would hide it. Equally, NOT `src/main/` + `src/preload/` the way the scaffolds\n(electron-vite, electron-forge) do: those folders exist because a bundler needs three build targets. We\nhave no bundler and no targets, so `main.js` and `preload.js` sit directly in `src/` — one level less,\nsame statement.\n\nNo `lib/` or `util/` bucket. What is left over (`perf.js`, `quit-guard.js`, `settings-transfer.js`,\n`env-refs.js`) joins the folder it belongs to or stays beside `main.js`. A handful of files next to the\nentry point is fine; a folder named \"misc\" is not.\n\n`main-lifecycle.js` does not survive as a file — its predicate joins the lifecycle code #213 extracts into\n`src/app/lifecycle.js`. Until then it moves to `src/` unchanged. CLAUDE.md's Architecture section is\ncorrected to describe the tree as it actually is.\n\n## What this touches beyond the moves\n\n- `package.json`: `\"main\": \"src/main.js\"`, and `build.files` gets SHORTER —\n  `[\"src/**/*\", \"build-info.json\", \"build/icon.*\", \"node_modules/**/*\"]`.\n- ~8 `__dirname` paths in `main.js`. Most keep working because their target moves along (`preload.js`,\n  `workers/`, `public/`→`renderer/`); `build/icon.png` stays outside `src/` and needs `'..'`, as does the\n  `cwd: __dirname` of the git call near the version handler.\n- 32 test requires: `require('../db')` → `require('../src/db')`.\n- `public/index.html`'s 69 `<script>` tags: unchanged — the folder moves whole, the relative paths inside\n  it still resolve.\n- `.github/workflows` and `scripts/`: nothing. Neither requires a root module.\n\n## Acceptance\n\n- No `.js` at the repo root; nothing under `src/` that is not app code.\n- No module's header comment contradicts its filename or its folder.\n- CLAUDE.md's Architecture section matches the tree, and every path it names resolves.\n- `npm test` green — `test/packaged-files.test.js` above all: `build.files` is an allow-list, and this is\n  exactly the change that drops code from the installer while the repo, `npm start` and the whole suite\n  stay green (0.7.5 shipped without `backends/` this way).\n- **The built installer starts and launches a session.** Not the repo — the installer. Close any running\n  Switchboard first, or the single-instance lock hands your launch to the installed app and you have\n  verified nothing.\n\nDo it as ONE move. Folders now and `src/` later means moving everything twice and breaking\n`git log --follow` twice.\n\nTiming: this rewrites `main.js`'s path and its `__dirname` lines, so it collides with any branch holding\n`main.js` open. Land it when nothing else does.\n\nRelated: #213 (fills `src/app/`), #215 (structures `src/renderer/`), #211 (`claude-config.js`).\n"}
{"number":215,"title":"public/ is 69 flat files and the bigger half of the app — give the renderer a structure","prio":"P3","labels":["chore"],"url":"https://github.com/deadeye636/switchboard/issues/215","refs":[213,214],"body":"`public/` holds **69 `.js` files, flat** — and it is the bigger half of the app:\n\n```\nmain process:  69 files, 20 840 lines\nrenderer:      69 files, 26 449 lines\n```\n\nFour of them carry a third of it: `app.js` (3188), `settings-panel.js` (2304), `sidebar.js` (2150),\n`grid-view.js` (1667). `main.js` at ~4970 is the loudest single file, but the renderer is the larger\nproblem, and it has been invisible because the root's clutter is what you see first.\n\nThe names are good and nearly every file states its job in a header. What is missing is any grouping:\n`a11y-utils.js`, `away-summary.js`, `bookmarks-logic.js`, `codemirror-setup.js`, `jsonl-search-logic.js`,\n`session-timeline.js` and `viewer-toolbar.js` are neighbours, and nothing says which of them belong\ntogether or which are load-bearing.\n\nRequirement: `src/renderer/` gets folders named after what they hold, and the files that grew into\nmonoliths are split the way `main.js` is in #213. Structure first, splits after — they are separate\npasses and separate reviews.\n\nDepends on #214, which moves `public/` → `src/renderer/` flat. This issue is what happens inside it.\n\n## Target\n\n```\nsrc/renderer/\n├── index.html  settings.html  style.css\n├── app.js                      3188 lines — its own split, after the move\n├── shell/                      sidebar, sidebar-state, view-menu, shortcuts, statusbar-zoom,\n│                               usage-status, update-restart, notification-policy, alert-sound,\n│                               attention-source, away-summary, bulk-actions\n├── session/                    session-card-details, -cleanup, -health, -history, -status,\n│                               -tabs, -timeline, -tags-filter, -tags-logic, subagent-live\n├── terminal/                   terminal-manager, -context-menu, -fit, -themes\n├── views/                      grid-view, grid-layout, stats-view, tasks-view, tasks-logic,\n│                               plans-memory-view, file-panel, viewer-panel, viewer-toolbar\n├── jsonl/                      jsonl-viewer, jsonl-search, jsonl-search-logic, preview-kind,\n│                               codemirror-setup\n├── panels/                     settings-panel, backends-panel, launchers-panel, variables-panel,\n│                               variables-admin, variable-insert, custom-launchers,\n│                               projects-admin, settings-window\n├── backends/                   backend-icons, backend-presets, backend-registry\n├── bookmarks/                  bookmarks-logic, bookmarks-view, bookmarks-tags,\n│                               project-tags-filter\n├── handoff/                    handoff, handoff-actions, handoff-extract\n├── dialogs/                    dialogs, control-dialogs\n└── lib/                        utils, a11y-utils, color-utils, icons, project-name, project-sort\n```\n\n`codemirror-bundle.js` is deliberately absent — it is an esbuild artifact, not source.\n\n## The catch: 69 `<script>` tags\n\nThere are no modules and no bundler here, by design (CLAUDE.md: don't add one). `index.html` loads all 69\nfiles as plain `<script>` tags and **the order carries meaning**. So every move is 69 paths to rewrite in\n`index.html` without disturbing the sequence. Mechanical, but this is the one place where the missing\nbundler costs real money, and a reordering bug here is silent until something is undefined at runtime.\n\nDo the move and the reordering in ONE commit, so a bisect lands either before or after, never inside.\n\n## Acceptance\n\n- No `.js` directly in `src/renderer/` except `app.js` (until its own split lands).\n- `index.html`'s script order is unchanged in EFFECT: the same files load in the same sequence, only from\n  new paths.\n- `npm test` green, `test/packaged-files.test.js` included — `build.files` is an allow-list and the\n  renderer is packaged through it.\n- **The app starts, the sidebar renders, a session opens, settings open, the JSONL viewer opens.** A\n  missing script tag does not fail a test; it fails a click. This is exactly the class of bug that shipped\n  green twice (CLAUDE.md).\n\n## Not in scope\n\nSplitting `app.js`, `settings-panel.js`, `sidebar.js` and `grid-view.js`. Each is its own pass once the\nfolders exist — bundling them into the move would produce a diff nobody can review.\n\nRelated: #214 (moves `public/` → `src/renderer/`), #213 (the main process's half).\n"}
